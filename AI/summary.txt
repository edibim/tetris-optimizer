2026-05-18
Prompt summary:
The user asked me to inspect the current repository, read and follow `AI/AGENTS.md` exactly, and act as a senior coding mentor so the project can progress step by step with the user writing most of the code with guidance. The user also asked that every prompt, together with a short summary of the answer, be recorded in `AI/summary.txt`.

Response summary:
An initial repository review was completed. There is no Go implementation yet, only guidance and project information files. The key rules from `AI/AGENTS.md` are: Go standard library only, small focused functions, clear separation of parsing/validation/solving/rendering, a backtracking solver, TDD whenever practical, and running `gofmt -w .`, `go vet ./...`, and `go test ./...` before commits. Our collaboration model from this point forward is mentor-style: we will choose the correct next step deliberately, avoid opaque copy-paste solutions, and keep the focus on understanding so the user can build similar projects independently by the end.

2026-05-18
Prompt summary:
The user clarified that everything written inside the repository must be in English, while the chat conversation should remain in Greek. The user asked me to fix `AI/summary.txt` accordingly and to create a proper senior-level PRD based on the full project information and our agreed mentorship approach.

Response summary:
`AI/summary.txt` was converted to English, and a structured PRD was prepared to guide the project. The PRD captures the product goal, scope, functional requirements, error handling expectations, technical constraints, recommended architecture, testing strategy, delivery milestones, acceptance criteria, and mentoring workflow so the implementation can proceed step by step with strong engineering discipline and clear learning outcomes.

2026-05-18
Prompt summary:
The user asked me to create the project structure and move into Milestone 1 implementation.

Response summary:
Milestone 1 project scaffolding was created. The repository now includes a Go module, a minimal `main.go` entrypoint, an `internal/` package layout (`errors.go`, `parser.go`, `validator.go`, `solver.go`, `board.go`, `tetromino.go`), and initial test folders under `tests/unit` and `tests/testdata`. The first unit tests were added for file reading, along with sample test fixtures. The codebase was formatted with `gofmt`, and verification was completed successfully with `go test ./...`, `go vet ./...`, and `go build ./...` using a writable Go cache path in `/tmp` because the default sandbox cache location was read-only.

2026-05-18
Prompt summary:
The user asked to proceed with the next implementation step.

Response summary:
The first parsing step of Milestone 2 was implemented. `ParseFile()` now performs strict file-format parsing: it rejects empty input, invalid separators, incomplete tetromino blocks, wrong line widths, and invalid characters. It also converts each 4x4 block into tetromino coordinates and assigns letters in input order. New unit tests were added to cover valid single and multiple tetromino inputs as well as key invalid format cases. The code was formatted with `gofmt`, and verification passed with `go test ./...` and `go vet ./...` using `GOCACHE=/tmp/go-build-cache`.

2026-05-18
Prompt summary:
The user asked to continue with the next implementation step after parsing.

Response summary:
The tetromino validation layer was implemented. `ValidateTetromino()` now enforces the core shape rules by rejecting tetrominoes that do not contain exactly four cells or are not orthogonally connected. `NormalizeTetromino()` was also implemented to shift tetromino shapes to the top-left origin and sort their coordinates deterministically. New unit tests were added for valid tetrominoes, invalid block counts, disconnected shapes, and normalization behavior. The code was formatted with `gofmt`, and verification passed with `go test ./...` and `go vet ./...` using `GOCACHE=/tmp/go-build-cache`.

2026-05-18
Prompt summary:
The user asked to continue after the validation layer was completed.

Response summary:
The parser and validator were integrated so that `ParseFile()` now returns only validated and normalized tetrominoes. Additional parser tests were added to verify normalization behavior and to ensure invalid tetromino shapes are rejected when parsing, including wrong block counts and disconnected shapes. The code was formatted with `gofmt`, and verification passed with `go test ./...`, `go vet ./...`, and `go build ./...` using `GOCACHE=/tmp/go-build-cache`.
